cmake_minimum_required(VERSION 3.7)
project(carfac_vendor)

# This is a placeholder. If you vendor the upstream CARFAC sources into
# libs/carfac/src, uncomment and adjust the following to build a STATIC lib.

# file(GLOB CARFAC_SRC CONFIGURE_DEPENDS src/*.cc)
# add_library(carfac STATIC ${CARFAC_SRC})
# target_include_directories(carfac PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/src)

# If you bring CARFAC via submodule (libs/carfac/upstream), add it here instead.
# add_subdirectory(upstream)

